# Model Weights — Not tracked by Git

Place your model files here before running the project:

- `full_CNN_model.h5`  — Lane detection CNN (Keras/TensorFlow)
- `best.pt`            — Pothole detection YOLOv8

These files are excluded from Git via `.gitignore`.
